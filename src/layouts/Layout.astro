<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/logo.svg" />
		<link rel="icon" href="/logo.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Agustin Aguero</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
   *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
:root {
  /* Fondo: Un gris grafito muy profundo (más suave que el negro) */
  --bg: #121212; 
  
  /* Superficies: Gris carbón con un toque de elevación */
  --surface: #1e1e1e; 
  
  /* Bordes: Muy sutiles para definir formas sin brillar */
  --border: rgba(255, 255, 255, 0.08); 
  
  /* Texto: Blanco suave (Off-white) */
  --text: #e5e5e5; 
  
  /* Muted: Gris piedra para info secundaria */
  --muted: #a3a3a3; 
  
  /* Acento: Ámbar suave (Dorado técnico) - Elegante y legible */
  --accent: #f59e0b; 

  --font-display: 'Playfair Display', Georgia, serif;
  --font-body: 'DM Sans', sans-serif;
  --nav-height: 72px;
}

    body {
      background: var(--bg);
      color: var(--text);
      font-family: var(--font-body);
      min-height: 100vh;
    }
    
</style>
